# dependencies
node_modules/

# capacitor 生成的 android 工程(CI 里构建,不入库)
android/

# 签名密钥(绝不入库,base64 存 GitHub Secret 备份在本地)
android-signing/

# build outputs
*.apk
*.aab

# misc
*.log
.DS_Store
Thumbs.db

# local tooling
.claude/
